//Credenciales de la base de datos
const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "",
        database: "bdgestion",
        connectTimeout: "60000"
    },
}

module.exports = config;